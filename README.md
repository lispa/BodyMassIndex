# BodyMassIndex

A simple console Body Mass Index (BMI) calculator written in Go.  
The program asks for **weight (kg)** and **height (cm)**, calculates BMI, and prints the **value** and **category**.